上传到github:  
// 用vscode打开项目并打开终端  
// git init  (首次)
git add .  
git commit -m "提交说明"  

git pull origin master （非首次）

// 将本地仓库与远程仓库关联  （首次）
// git remote add origin http://github.com/Zoey-unavailable/phoneval.git

// 将本地提交推送到GitHub  
// git push -u origin master （首次）
git push origin master
// git pull origin master --allow-unrelated-histories

